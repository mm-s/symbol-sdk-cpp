/*
#include "core.h"

using namespace std;
using namespace symbol;

typedef symbol::mosaic c;

void c::to_stream(ostream& os) const {
    using namespace catapult::utils;
    os << "mosaic: " << MosaicId << " amount: " << Amount << '\n';
}

ostream& operator << (ostream& os, const c& o) {
    o.to_stream(os);
    return os;
}

*/
